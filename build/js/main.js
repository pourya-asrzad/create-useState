"use strict";
console.log("arman");
class useState {
    constructor(initialValue, setterFunc) {
        this.initialValue = initialValue;
        this.initialValue = initialValue;
    }
    get StateValue() {
        return this.initialValue;
    }
    setter(initialValue) { }
}
